<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>


        <property name="connection.driver_class">${db.driverClass}</property>
        <property name="dialect">${db.hibernate.dialect}</property>

        <property name="connection.url">${db.connectionURL}</property>
        <property name="connection.username">${db.username}</property>
        <property name="connection.password">${db.password}</property>

        <property name="show_sql">${db.hibernate.show_sql}</property>


        <!--<property name="connection.url">jdbc:mysql://localhost:3306/auction</property>-->
        <!--<property name="connection.driver_class">com.mysql.jdbc.Driver</property>-->
        <!--<property name="connection.username">auctionadmin</property>-->
        <!--<property name="connection.password">123123</property>-->
        <!--<property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>-->
        <!--<property name="show_sql">true</property>-->

        <!-- DB schema will be updated if needed -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="ws.model.User"/>
        <mapping class="ws.model.Lot"/>
        <mapping class="ws.model.Bid"/>

    </session-factory>

</hibernate-configuration>